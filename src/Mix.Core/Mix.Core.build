<?xml version="1.0"?>
<project name="Mix.Core" default="build">

  <property name="name" value="Mix.Core"/>

  <target name="build" description="Builds the Mix.Core assembly">
    <csc target="library" warnaserror="true" debug="true" output="${build.dir}/${name}.dll">
      <sources>
        <include name="**/*.cs" />
        <include name="../CommonAssemblyInfo.cs" />
      </sources>
      <references>
        <lib>
          <include name="${build.dir}" />
          <include name="${lib.dir}" />
        </lib>
      </references>
    </csc>
  </target>

</project>
