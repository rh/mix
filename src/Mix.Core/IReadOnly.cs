namespace Mix.Core
{
    /// <summary>
    /// A marker interface to indicate that an <see cref="ITask"/> does not
    /// change the XML.
    /// </summary>
    public interface IReadOnly
    {
    }
}