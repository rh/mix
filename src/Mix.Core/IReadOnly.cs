namespace Mix.Core
{
    /// <summary>
    /// A marker interface to indicate that an <see cref="IAction"/> does not
    /// change the XML.
    /// </summary>
    public interface IReadOnly
    {
    }
}